name: car_diagnostic_ai
description: تطبيق تشخيص سيارات OBD-II بالذكاء الاصطناعي - النسخة الاحترافية
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.32.12
  google_generative_ai: ^0.4.6
  sqflite: ^2.3.3
  path_provider: ^2.1.3
  permission_handler: ^11.3.1
  geolocator: ^11.0.0
  geocoding: ^3.0.0
  url_launcher: ^6.2.6
  fl_chart: ^0.68.0
  shared_preferences: ^2.2.3
  intl: ^0.19.0
  provider: ^6.1.2
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true